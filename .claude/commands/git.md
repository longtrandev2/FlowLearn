Your goal is:
- Run `git status` to check status of files in the task
- Run `git add` + `all file have been modifiled in the current task`
- Write the best git commit and Run `git commit -m "here is the commit of this task"`
- Run `git push -u origin HEAD`